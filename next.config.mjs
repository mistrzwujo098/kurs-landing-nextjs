/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/egzamin',
  assetPrefix: '/egzamin',
  images: {
    unoptimized: true,
  },
}

export default nextConfig
